return {
  {
    'f-person/git-blame.nvim',
    -- version = '*',
    -- config = function()
    --   require('bufferline').setup {
    --     options = {
    --       --Note how the `gitblame_` prefix is omitted in `setup`
    --       enabled = false,
    --     },
    --   }
    -- end,
  },
}
